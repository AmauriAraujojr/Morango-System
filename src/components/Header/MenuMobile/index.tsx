import { HeaderControls } from "../HeaderControls"
import { StyledMenuMobile } from "./style"

export const MenuMobile=()=>{

    return(
        <StyledMenuMobile>
            <HeaderControls/>
        </StyledMenuMobile>
    )
}